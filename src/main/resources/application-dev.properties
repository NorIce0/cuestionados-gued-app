# DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)

# JDBC URL of the database.
spring.datasource.url= jdbc:postgresql://ec2-184-73-25-2.compute-1.amazonaws.com:5432/d71tcdv3e663e
# Login username of the database.
spring.datasource.username= qozosksdtrqmeq
# Login password of the database.
spring.datasource.password= bf56fc8023e8c53ff72df1f1384c4a5b6b54b2a4c8fa574454319b1a17a838a6

# JPA (JpaBaseConfiguration, HibernateJpaAutoConfiguration)

# DDL mode. This is actually a shortcut for the "hibernate.hbm2ddl.auto" property. Defaults to "create-drop" when using an embedded database and no schema manager was detected. Otherwise, defaults to "none".
spring.jpa.hibernate.ddl-auto= update

# Whether to enable logging of SQL statements.
spring.jpa.show-sql=true

# Hibernate additional native properties to set on the JPA provider.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.current_session_context_class=thread
spring.jpa.properties.hibernate.format_sql=true

# Fix Postgres JPA Error (Method org.postgresql.jdbc.PgConnection.createClob() is not yet implemented).
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true

# App Properties
norice0.app.jwtSecret= NorIce0SecretKey
norice0.app.jwtExpirationMs= 86400000
